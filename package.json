{
  "name": "tabella",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @tabella/web",
    "build:web": "npm run build --workspace @tabella/web",
    "lint:web": "npm run lint --workspace @tabella/web"
  }
}

